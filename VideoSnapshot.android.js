/**
 * Stub of VideoSnapshot for Android.
 *
 * @providesModule VideoSnapshot
 * @flow
 */
'use strict';

var warning = require('warning');

var VideoSnapshot = {
  test: function() {
    warning('Not yet implemented for Android.');
  }
};

module.exports = VideoSnapshot;
